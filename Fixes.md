Remove "Part of a series" component from below "event details" in event detail page. Move it to the sidebar, below "about the organizer"
Remove the tags component from below "event details", move it to the very bottom of the page

In /events, in the filter bar, move the location filter as the top first one. Keep the rest as they are.

In org/slug/evt-id/tickets, in the ticket list endpoint, the schema will now provide a stripe_dashboard_url for online payments. Use that in the corresponding link component when needed